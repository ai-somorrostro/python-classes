version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Puedes exportar OPENROUTER_API_KEY en tu entorno o usar .env
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    restart: unless-stopped
